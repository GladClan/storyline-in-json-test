import { pointerType } from "./pointerFunctions";


export async function battle(enemyrefs: string[], pointers: pointerType, target?: string, ref?: string | boolean) {
   //
}


/**
 * Setup:
    * populate enemies from a list
       * pass in enemy references and then look up the objects for those references in a dictionary
    * give enemies ai, fulfill target parameters
    * ref or next-scene
    * set pointer
 */